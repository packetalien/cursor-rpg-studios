# Hardware / OpenClaw (placeholder)

- **OpenClaw** job types: TBD (batch generation, validation swarms, ingest).
- **Secrets:** never in-repo; use `~/.secrets` per operator policy.

Runbook: [`docs/hardware-runbook.md`](../docs/hardware-runbook.md).
