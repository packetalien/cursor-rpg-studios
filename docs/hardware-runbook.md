# Hardware runbook (placeholder)

- **OpenClaw:** batch job types, machine roles, and secrets live **outside** the repo (`~/.secrets`).
- **Operator paths:** align with sibling Alexandria / Ironman docs when wired.

See [`hardware/README.md`](../hardware/README.md).
